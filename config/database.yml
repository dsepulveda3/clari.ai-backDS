default: &default
    host: localhost
    adapter: postgresql
    encoding: unicode
    username: postgres
    password: postgres
    # username: "clari_next"
    # password: "Sepulveda091098"
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
    <<: *default
    database: postgres

test:
    <<: *default
    database: postgres

production:
    <<: *default
    host: postgres
    database: postgres